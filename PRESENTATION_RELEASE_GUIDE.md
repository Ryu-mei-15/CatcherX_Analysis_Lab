# EC2026発表資料の公開切替手順

発表資料は`presentation-config.js`で資料ごとに公開状態を管理します．現在は口頭発表スライドとポスターの両方を公開しています．

## 現在の状態

- ポスターは公開中（1ページ，約4.1 MB）
- スライドは公開中（22ページ，約19.4 MB）
- `paper.html`では両方に閲覧ボタンを表示
- 発表資料と同じフォルダにある動画は公開対象外

## 発表資料を差し替える手順

1. PDFを差し替え，ページ抜けや表示崩れがないか全ページを確認する．
2. `presentation-config.js`の`pages`と`fileSize`を最終PDFに合わせる．
3. PDFと設定ファイルをGitへ追加する．
4. ローカルHTTPサーバで`paper.html`を開き，両方の「ブラウザ内で閲覧する」が動作することを確認する．
5. GitHub Pagesへ反映後，公開URLからPDFが表示できることを確認する．

## 公開を再び停止する場合

対象資料の`published`を`false`へ戻すだけでは，すでに配信したPDFへの直接アクセスは残ります．PDFを公開履歴から完全に消す必要がある場合は，GitHub Pagesの配信元とGit履歴を含めた別対応が必要です．通常は，公開前にPDFをコミットしない運用を徹底してください．
